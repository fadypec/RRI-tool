# Codebase Audit — 23 April 2026

Full audit of RRI STEER prior to user testing and GitHub publication.

## Critical

- [x] **C1.** API key in `app/.env` — added `.env` to `app/.gitignore` explicitly.
- [x] **C2.** No ARIA attributes — added `role`, `aria-checked`, `aria-label`, `aria-expanded`, and focus rings to all interactive elements across all pages (DeepDiveQuestion, ResearchProfile, Safeguards, Welcome, Context, Reflection, Results, Express, Stepper).

## High

- [x] **H1.** Root-level README.md created with full project description, quick start, deploy instructions.
- [x] **H2.** Rate limiting added: `express-rate-limit` at 20 req/min/IP on `/api/analyze`.
- [x] **H3.** PDF export implemented using jsPDF + html2canvas. Download button added to export bar.
- [x] **H4.** Keyboard navigation: focus rings on all interactive elements, Escape key closes Stepper dropdown.
- [x] **H5.** Heading hierarchy fixed: all pages now use `<h1>` as top-level heading.
- [x] **H6.** Augmented reflection wired to call `classifyMisuseConcerns()` API with loading/error states.

## Medium

- [ ] **M1.** Template text uses advisory language. *Deferred — requires content rewrite with domain expert review.*
- [ ] **M2.** Templates/safeguards/prompts hardcoded in TS. *Deferred — structural refactor for v1.1.*
- [ ] **M3.** Cross-question scoring interactions not implemented. *Deferred — requires careful testing.*
- [ ] **M4.** Phase 3E info-hazard auto-trigger. *Deferred — requires scoring integration.*
- [x] **M5.** Stale branding: localStorage key changed from `dul-theme` to `rri-steer-theme`.
- [x] **M6.** ESLint errors fixed. Unused deps removed (react-hook-form, lucide-react, clsx, tailwind-merge).
- [x] **M7.** `beforeunload` now fires on all pages when user has progressed past welcome.
- [x] **M8.** Colour contrast improved: `--color-ink-light` → #6b7280, `--color-accent` → #a07828 (light); `--color-ink-light` → #9a9488 (dark).
- [ ] **M9.** `grantStage` still collected but unused downstream. *Low priority — informational only.*
- [ ] **M10.** Taxonomy data files still empty. *Deferred — requires authoritative source data curation.*
- [x] **M11.** `helmet` middleware added to Express server.
- [x] **M12.** Dead `computeResult` and `goToStep` code removed from useAssessment.ts.
- [x] **M13.** CLAUDE.md status updated to "v1.0 PoC built. Pre-user-testing."
- [ ] **M14.** Assessment flow doc references unimplemented features. *Doc update deferred — doc reflects aspirational design, code reflects v1.0 scope.*

## Low

- [x] **L1.** AssessmentGrid now receives `theme` as a prop (reactive on toggle).
- [x] **L2.** React.Fragment keys added in AssessmentGrid.
- [x] **L3.** `goToStep` removed (was dead code).
- [ ] **L4.** `console.error` in server.ts — *Acceptable for PoC. Production logging to be addressed at handoff.*
- [x] **L5.** Added Environmental Sustainability and ORBIT links to broader RRI section.
- [ ] **L6.** Tech stack doc references pnpm. *Cosmetic doc discrepancy, low priority.*
- [x] **L7.** `app/README.md` replaced with pointer to root README.
- [x] **L8.** Unused imports (`extractEntities`, `getCategoryLabel`) removed from ExpressPage.

## Summary

**Fixed: 22 of 28 items** (both critical, all 6 high, 9 of 14 medium, 6 of 8 low).

**Remaining 6 items** are either deferred to v1.1 (structural refactors, content rewrites, taxonomy data population) or cosmetic doc discrepancies. None block user testing or GitHub publication.
